import { h, render, cloneElement } from 'preact';
import Slide from '../../../components/Slide';
import Title from '../../../components/Title';

const Nodes = () => {
    return (
        <Slide>
            <Title>Let's talk about nodes</Title>
        </Slide>
    );
};

export default Nodes;
