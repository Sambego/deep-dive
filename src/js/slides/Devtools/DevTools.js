import { h, render } from 'preact';
import Slide from '../../components/Slide';
import Subtitle from '../../components/Subtitle';

const Devtools = () => {
    return (
        <Slide>
            <Subtitle>What about debugging?</Subtitle>
        </Slide>
    );
};

export default Devtools;
